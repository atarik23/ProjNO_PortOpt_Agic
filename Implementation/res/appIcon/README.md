# Application icon

The project-specific Windows `.ico` and macOS `.icns` assets will be added after the visual identity is finalized. The unrelated icon from the reference `res.zip` is intentionally not reused.
